# Pokemon Red AI - Python dependencies
#
# Primary path: PPO (Stable-Baselines3 + Gymnasium + PyBoy)
# Legacy path:  Rainbow DQN (extra code under src/workers, src/pokemon/ModelPokemon.py)

torch>=2.1.0
numpy>=1.24
pyboy>=2.0.0
gymnasium>=0.29.0
stable-baselines3>=2.3.0
sb3-contrib>=2.3.0  # MaskablePPO -- hard-masks illegal actions (e.g. NONE in dialog/battle/menu)
tensorboard>=2.14
keyboard>=0.13.5
tqdm>=4.66.0
rich>=13.0.0
matplotlib>=3.7
pillow>=10.0  # pokemon.map_render: decodes reference/pokered's tile-sheet PNGs for the heatmap background
